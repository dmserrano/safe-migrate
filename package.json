{
  "name": "safemigrate",
  "version": "0.0.1",
  "description": "Migration-aware characterization test generator for legacy JS codebases",
  "type": "module",
  "bin": { "safemigrate": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/cli.js",
    "test": "node --test",
    "prepublishOnly": "npm run build"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "commander": "^12.0.0",
    "execa": "^9.0.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  },
  "license": "MIT"
}
